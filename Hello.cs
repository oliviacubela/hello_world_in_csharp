using System;
// using System is called a directive... because C# is compiled, we need to tell it what code it needs in order to function.

class Program
{
//  This is an entry point - methods that automatically run when a program is launched are called entry points, like static void Main()
  static void Main()
  {
    Console.WriteLine("Hello World!");
  }
}